#ifndef _summon_h
#define _summon_h


void SummonBlock(double x, double y,MODE mode);
//given the position x,y to build the block with the size of LEVEL and MODE setting

inline void setBlockInf(double x, double y,MODE mode);
//create the block element






#endif
